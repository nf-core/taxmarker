{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/taxmarker/master/nextflow_schema.json",
    "title": "nf-core/taxmarker pipeline parameters",
    "description": "This pipeline checks sequences for their phylogenetic signal against their taxonomy",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["outdir"],
            "properties": {
                "taxonomy": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "pattern": "^\\S+\\.(tax)|(tsv)",
                    "description": "Path to a taxonomy file. If omitted, taxonomy is derived from --sequences record headers instead.",
                    "help_text": "TSV file with two columns: Name and semicolon-separated taxonomy. If not given, each --sequences FASTA record's header must carry the taxonomy directly after its id (GTDB-style: `>id taxonomy;string`). If both are given, the file wins -- a warning is logged if header text is also present and being ignored.",
                    "fa_icon": "fas fa-file-alt"
                },
                "sequences": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "pattern": "^\\S+\\.(aln)|(phy)|(alnf[na]a)|(mfa)|(f[na]a)|(fa)|(fasta)$",
                    "description": "Path to a sequences file, aligned or not, in phylip (.phy), Clustal (.aln) or fasta format (.fa, .fasta, .mfa, .alnfaa or .alnfna).",
                    "help_text": "Unaligned content is aligned automatically via hmmalign -- see --hmm/--hmm_name.",
                    "fa_icon": "fas fa-file-alt"
                },
                "hmm": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to an HMM profile database, used to align --sequences via hmmalign if it turns out to be unaligned. Only required when the input is unaligned.",
                    "help_text": "Not needed at all if --sequences is already a multiple sequence alignment (detected automatically). If the database holds more than one named profile, also set --hmm_name.",
                    "fa_icon": "fas fa-dna"
                },
                "hmm_name": {
                    "type": "string",
                    "description": "Name of the specific profile to use from --hmm, if that database holds more than one (e.g. Barrnap's databases hold one profile per rRNA gene).",
                    "fa_icon": "fas fa-tag"
                },
                "seqgrep": {
                    "type": "string",
                    "description": "Keep only --sequences records whose whole FASTA header matches this pattern, dropping the rest before anything else runs.",
                    "help_text": "Passed to `seqkit grep -n -r -p`, so it's a regular expression matched against each record's whole header line (id plus any trailing text), not just the id. Useful for restricting a shared/combined reference set -- e.g. GTDB's own ssu_all distribution, which covers more than one domain in a single file -- to just the records wanted, by matching against embedded text such as `d__Archaea`.",
                    "fa_icon": "fas fa-filter"
                },
                "upstream": {
                    "type": "string",
                    "enum": ["GTDB"],
                    "description": "Name of an upstream data source this run's --sequences/--taxonomy came from, to enable source-specific pipeline behaviour. Currently only 'GTDB' is recognised: translates --gtdb_bac120_metadata/--gtdb_ar53_metadata into --sequence_weights automatically.",
                    "fa_icon": "fas fa-database"
                },
                "gtdb_bac120_metadata": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "GTDB bacterial (bac120) genome metadata table (gzip-ok). Required (with/without --gtdb_ar53_metadata) when --upstream is 'GTDB'.",
                    "help_text": "GTDB's own per-release `bac120_metadata_*.tsv[.gz]` file, unmodified. See nf-core/taxmarker#15 for the weight formula this feeds.",
                    "fa_icon": "fas fa-table"
                },
                "gtdb_ar53_metadata": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "GTDB archaeal (ar53) genome metadata table (gzip-ok). Required (with/without --gtdb_bac120_metadata) when --upstream is 'GTDB'.",
                    "help_text": "GTDB's own per-release `ar53_metadata_*.tsv[.gz]` file, unmodified. See nf-core/taxmarker#15 for the weight formula this feeds.",
                    "fa_icon": "fas fa-table"
                },
                "skip_clustering": {
                    "type": "boolean",
                    "description": "Skip WEIGHTED_CLUSTERING entirely and run every sequence straight through, unclustered.",
                    "fa_icon": "fas fa-forward"
                },
                "sequence_weights": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "description": "Path to a two-column table (sequence_id<TAB>weight) used to pick a representative sequence per (cluster, taxon) pair. Sequences absent from the table default to weight 1.",
                    "help_text": "The clustering/selection logic only ever sees this generic table -- it doesn't matter where the weights came from (e.g. a GTDB-metadata-derived translation, see --upstream). Omit entirely to treat every sequence as weight 1, which degrades gracefully to picking the longest sequence per (cluster, taxon) pair. If both this and --upstream are given, this file wins (with a warning), the same precedent as --taxonomy winning over embedded taxonomy text.",
                    "fa_icon": "fas fa-balance-scale"
                },
                "min_weight": {
                    "type": "number",
                    "description": "Absolute weight cutoff: sequences below this are excluded before clustering, regardless of --sequence_weights. Unset by default -- has no effect unless both this and --sequence_weights are given.",
                    "help_text": "Relative per-cluster selection alone can't exclude a badly-mislabeled sequence that never co-clusters with a competitor (e.g. a singleton cluster) -- this cutoff is the only mechanism that can. No default at the generic-mechanism level, by design: whichever weight-producing translation supplies --sequence_weights (e.g. a GTDB-specific one) is responsible for recommending its own cutoff, calibrated to its own scale.",
                    "fa_icon": "fas fa-cut"
                },
                "cluster_identity": {
                    "type": "number",
                    "default": 1.0,
                    "minimum": 0,
                    "maximum": 1,
                    "description": "VSEARCH clustering identity threshold (0-1) applied before representative selection.",
                    "help_text": "1.0 (the default) is a safe no-op: pure dereplication of exact/near-identical duplicates. Lower it for more aggressive input-size reduction on very large datasets.",
                    "fa_icon": "fas fa-object-group"
                },
                "skip_raxtax": {
                    "type": "boolean",
                    "description": "Skip the raxtax prefilter and run every sequence straight through the full EPA-ng-based placement pipeline.",
                    "fa_icon": "fas fa-forward"
                },
                "raxtax_filter_rank": {
                    "type": "integer",
                    "default": 1,
                    "description": "Taxonomic rank at which raxtax's self-classification must agree with the declared taxonomy, counted from the most specific rank (1) upward. A sequence disagreeing at this one rank is flagged as a mislabel and skips EPA-ng placement.",
                    "help_text": "A sequence whose taxon at this rank has no other sequence in the input is not checked: raxtax never sees the sequence itself when classifying it, so it cannot predict that taxon.",
                    "fa_icon": "fas fa-layer-group"
                },
                "skip_gapfilter": {
                    "type": "boolean",
                    "description": "For already-aligned input: skip filtering out sequences with too high a proportion of alignment gaps, and run every sequence straight through the rest of the pipeline.",
                    "fa_icon": "fas fa-forward"
                },
                "min_nongap": {
                    "type": "number",
                    "default": 0.3,
                    "description": "For already-aligned input: minimum proportion of non-gap columns a sequence must have in the alignment to be kept. Sequences below this are too short/incomplete to place reliably, and are reported separately instead of being placed.",
                    "help_text": "A taxonomically broad reference alignment is naturally wide, with many columns that are only real sequence for a handful of divergent taxa -- a low default keeps that from penalising genuinely full-length sequences. Raise it for a narrower (e.g. single-genus) alignment where a stricter threshold makes sense. Only applies to input that was already aligned on entry -- see --min_profile_cover for hmmalign-derived input.",
                    "fa_icon": "fas fa-ruler"
                },
                "skip_profile_cover": {
                    "type": "boolean",
                    "description": "For hmmalign-derived input: skip filtering out sequences that cover too little of the HMM profile, and run every sequence straight through the rest of the pipeline.",
                    "fa_icon": "fas fa-forward"
                },
                "min_profile_cover": {
                    "type": "number",
                    "default": 0.8,
                    "description": "For hmmalign-derived input: minimum proportion of the HMM profile's match-state columns a sequence must cover to be kept. Sequences below this are too short/incomplete to place reliably, and are reported separately instead of being placed.",
                    "help_text": "Unlike an already-aligned reference set, a masked hmmalign-derived alignment's width is fixed by the HMM profile itself, so real full-length sequences cluster tightly near full coverage -- a higher default than --min_nongap correctly separates them from genuinely partial sequences instead of barely filtering anything.",
                    "fa_icon": "fas fa-ruler"
                },
                "skip_sativa": {
                    "type": "boolean",
                    "description": "Skip the phylogenetic placement subworkflow (reference tree, leave-one-out placement, scoring) entirely.",
                    "help_text": "Turns the rest of the pipeline into a general-purpose taxonomy-resolution/alignment/prefilter QC tool: taxonomy resolution, name-consistency checking, optional alignment (via hmmalign) and gap/profile-coverage filtering, and the optional raxtax prefilter all still run as configured. The final mislabels report then contains only raxtax-flagged mislabels (or is empty if --skip_raxtax is also set) -- no EPA-ng-based placement is performed. The cleaned, filtered alignment and taxonomy are still published, under whichever upstream step (raxtax prefilter, gap/profile-coverage filter, or name-consistency check) produced them last.",
                    "fa_icon": "fas fa-forward"
                },
                "taxcode": {
                    "type": "string",
                    "default": "bac",
                    "enum": ["bac", "bot", "zoo", "vir"],
                    "description": "Taxonomic code of the input data, passed to sativa-epang: bac(teriological), bot(anical), zoo(logical) or vir(ological). Governs its nomenclature-revision suggestions.",
                    "fa_icon": "fas fa-dna"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                }
            }
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "multiqc_logo": {
                    "type": "string",
                    "description": "Custom logo file to supply to MultiQC. File name must also be set in the MultiQC config file",
                    "fa_icon": "fas fa-image",
                    "hidden": true
                },
                "multiqc_methods_description": {
                    "type": "string",
                    "description": "Custom MultiQC yaml file containing HTML including a methods description.",
                    "fa_icon": "fas fa-cog"
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "pipelines_testdata_base_path": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "Base URL or local path to location of pipeline test dataset files",
                    "default": "https://raw.githubusercontent.com/erikrikarddaniel/test-datasets/",
                    "hidden": true
                },
                "trace_report_suffix": {
                    "type": "string",
                    "fa_icon": "far calendar",
                    "description": "Suffix to add to the trace report filename. Default is the date and time in the format yyyy-MM-dd_HH-mm-ss.",
                    "hidden": true
                },
                "help": {
                    "type": ["boolean", "string"],
                    "description": "Display the help message."
                },
                "help_full": {
                    "type": "boolean",
                    "description": "Display the full detailed help message."
                },
                "show_hidden": {
                    "type": "boolean",
                    "description": "Display hidden parameters in the help message (only works when --help or --help_full are provided)."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
